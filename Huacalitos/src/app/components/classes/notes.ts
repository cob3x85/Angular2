import { NoteTypeEnum } from 'app/components/classes/note-type-enum';

export class Notes {
    public fromName: string;
    public NoteType: NoteTypeEnum;
    public title: string;
    public toName: string;
}
